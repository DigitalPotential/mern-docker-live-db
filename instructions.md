add .env file in root with MONGODB_URL=connection string from https://www.mongodb.com/

add .env file in backend folder with DB_URL=connection string from https://www.mongodb.com/

run with:
docker compose up
